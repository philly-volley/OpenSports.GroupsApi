namespace OpenSports.GroupsApi.Constants
{
    public static class EventsControllerRoute
    {
        public const string GetEvent = ControllerName.Events + nameof(GetEvent);
        public const string GetEvents = ControllerName.Events + nameof(GetEvents);
    }
}
